import Vuetify from "vuetify";
import Vue from "vue";

Vue.use(Vuetify);

export default new Vuetify({
  // icons: {
  //     iconfont: 'fa' || 'mdiSvg' || 'mdi' || 'mdiSvg' || 'md' || 'fa' || 'fa4' || 'faSvg'
  // }
});
